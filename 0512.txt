/*
    add : '추가'했다는 뜻입니다.
    fix : '수정'했다는 뜻입니다.
*/


add : 일단 전체적으로 전처리 코드들을 넣어두었습니다. 앞으로 함수 작성을 진행하도록 하겠습니다.
아래는 부연설명입니다.

add : vk_space는 매크로로 정의된 키코드이며, 
값은 32로 설정되어 있습니다. 
이는 ASCII 코드로 공백 문자(space)에 해당합니다. 따라서 vk_space는 공백 키를 나타내는 데 사용됩니다.
이외 vk_enter도 마찬가지입니다. (vk는 virtual key의 약자입니다.)

fix : 교수님께서 score를 추가해보면 어떻냐 해서 Game Global var 에 score를 일단 추가하였습니다.

---------- (부연 설명 끝)

fix : 전체적으로 코드 구조를 다듬었습니다.

add : Menu Functions쪽을 코딩하였습니다. 로고를 띄우게 하였으며,
로고의 가로 사이즈와 세로 사이즈를 구하여 draw_logo함수에 구현하였습니다.
*(참고) attron과 attroff함수는 컬러를 설정하는 함수입니다. (터미널 색상을 변경할 때마다 색상이 달라지게 하는 것입니다)

add : main함수를 추가하여 임시로 디버깅할 수 있게 만들었습니다.

add : 메인함수
            (1) Start curses mode
        -> 키보드에 있는 화살표 등을 입력받기 위해 keypad() 코드를 넣었습니다.
            종료 후에도 현재 터미널 설정을 유지하기 위해 savetty() 코드와 resetty() 코드를 넣었습니다.
            사용자가 키를 누를때마다 즉시 입력처리 될 수 있도록 cbreak() 코드를 넣었습니다. 이는 캐논모드(버퍼에 저장하는 것)를 해제하는 코드입니다.
            사용자가 키 입력시 화면에 보이지 않게 noecho()함수를 넣었습니다.
            사용자가 키 입력을 안해도 프로그램이 실행될 수 있도록 timeout(0) 코드를 넣었습니다. *(참고)비차단(non-blocking) 입력 처리
            커서의 위치를 숨기기 위해 leaveok() 코드를 넣었습니다.
            시드를 초기화하기 위해 srand(time(NULL)) 코드를 넣었습니다.

            (2) 미로 초기화 코드를 넣었습니다.



/*
    0513(화) 오후 2시 8분경에 수정하였으며, 키를 입력하면 endwin()이 호출되어 종료되기 때문에
    시작 화면 그래픽 구현부터 해보고 기능은 추후에 넣도록 하겠습니다.
*/

